---
description: Prepare an evidence-backed brief, then ask the isolated expert panel
argument-hint: "[focus or artifact]"
---

Load and follow the `second-opinion` skill. Prepare the review context in the parent model before calling its expert-panel tool.

The user supplied this optional focus or artifact:

```text
${ARGUMENTS:-No explicit focus supplied. Resolve the subject from the active conversation.}
```
